{
  "name": "cctsystem-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "smoke:node": "node dev/mock-minecraft-node.mjs",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && npm run test && wrangler deploy --dry-run --outdir dist"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260814.1",
    "@types/ws": "^8.18.1",
    "typescript": "~6.0.2",
    "vitest": "4.1.10",
    "wrangler": "4.123.0",
    "ws": "^8.21.3"
  }
}
